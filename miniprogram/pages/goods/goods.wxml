<!--pages/goods/goods.wxml-->
<view wx:for="{{list}}">
<view class="item" bindtap="goDetail" data-name="{{item.name}}">
<image src="{{item.img}}" class="img"></image>
<text>商品名:{{item.name}}， 价格{{item.price}}</text>
</view>
</view>